import classes from "@/styles/Home.module.css";
import { Container, Grid, Row, Link } from "@nextui-org/react";

const Footer = () => {
  return (
    <Container fluid>
      <div className={classes.myfooter}>
        <h1>Footer</h1>
      </div>
    </Container>
  );
};

export default Footer;
